/**
 * Top-Level Library Namespace
 */
/*global require */
/** @namespace */
var bookworm = {},
    dessert = dessert || require('dessert'),
    troop = troop || require('troop'),
    sntls = sntls || require('sntls'),
    flock = flock || require('flock');
